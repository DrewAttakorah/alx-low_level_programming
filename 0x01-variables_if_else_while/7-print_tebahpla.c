#include <stdio.h>
/**
 * main-main code block in c
 * Description: Prints alphabets from the back
 * Return: 0
 */
int main(void)
{
	char ant = 'z';

	for (ant = 0; ant <= 0; ant--)
	{
		putchar(ant);
	}
	putchar('\n');

	return (0);
}
